# היום שלי

PWA אישית ל-ADHD. עברית, RTL, אופליין. `app.js` הוא כל האפליקציה, וניל JS, בלי build.

## פקודות
- הרצה: `python3 -m http.server 4180 --directory ~/my-day` ✅ עובד
- **`node shots.mjs` שבור.** Chromium לא מותקן. `screens/` תקוע ב-25.07 ואסור להציג
  אותו כראיה. עד שיתוקן, מסך נראה מיוצר בדפדפן הפנימי בלבד.
- אין build, אין טסטים, אין linter, אין git

## מקורות אמת, לפי סדר. הראשון גובר על כולם

1. **`plans/DECISIONS.md`** — הצהרות של חן במילים שלה, מתוארכות.
   חולץ מ-`.file-versions` בפרויקט Open Design.
2. **המסך החי (`.html`).** פרויקט Open Design בשם `brand-product-a4813f`.
   **זה מקור האמת לעיצוב.**
3. `plans/MAP.md` — כל המסכים, הפלאוו, הסתירות הפתוחות
4. `plans/design-system.md` · `plans/microcopy-guide.md`

**מסך או מסמך שסותר שורה ב-`DECISIONS.md`, הוא זה שטועה.**

**`_archive-old-mockups/` הם צילומי טסט מגרסת הקוד הישנה של האפליקציה.**
לעיון בלבד. **לא** מקור אמת, **לא** עיצובים של חן, ולא נמחקים.
בלשונה, 30.7: *"המסכים הכחולים לא עוצבו כאן, הם היו טסטים כשיצרתי את
האפליקציה בקוד. ברור שכל העיצובים החיים הם המקוריים."*

`screen-map.md` ו-`design-system.md` מכריזים בראשם "מקור אמת יחיד". **הם לא.**
`compound-effect-flow.md`, `screen-map.md`, `ux-review.md` — מיושנים, לא לבנות לפיהם.

## ארכיטקטורה
- `app.js` — מצב, ניתוב, רינדור, אירועים
- `knowledge.js` — תוכן מגרסה קודמת של המוצר. **לאמת מול `DECISIONS.md` לפני שמצטטים**
- `styles.css` — הסגנון של האפליקציה. **`habit-flow.css` הוא של המוקאפים ב-Open Design, קובץ אחר**
- מצב ב-localStorage תחת `my-day-v1`

## חוקים

כל כלל נפתח ברגע שבו הוא נדלק. כלל שנשבר, כתוב מתי ואיך, כדי שלא ייטען שוב
שהוא "מובן מאליו".

**כשאני מתחיל משימה, לפני הפעולה הראשונה:** עונה לעצמי על שתי שאלות.
**מה יתפוס אותי אם אני טועה כאן**, ו**האם השיטה שבחרתי יכולה בכלל למצוא את סוג
הטעות הזה**. אם התשובה השנייה היא לא, השיטה פסולה גם אם היא מהירה.

זה הכלל שמייצר את כל השאר. שלושת הכשלים של 30.7 הם אותו כשל: בחרתי שיטה לפי
מה שנוח לי ולא לפי מה שהשאלה דורשת. מקבילות במקום דפדפן. `grep` במקום ערך מחושב.
סף של 400 פיקסלים במקום השאלה מה מכסה את מה. בשלושתם השיטה לא יכלה למצוא את
הטעות, וזה היה ידוע מראש.

**האחריות על איתור הטעויות היא שלי. חן היא לא שכבת הבדיקה.**
כשחן מוצאת טעות או מציעה דרך עבודה נכונה יותר, זה כשל שלי, לא שלה. התגובה
היא לתקן את השיטה, לא להסכים שהיא צודקת.

**כשחן מבקשת משהו:** אני מנהל את זה עד הסוף ומראה לה תוצר. **חן לא מעורבת באמצע.**

**עוצרים ומחכים למילה "מאושר" רק בקריטי**, כלומר מה שקשה להחזיר או שההחלטה
בו שלה:
מחיקה או ארכוב של קובץ · `app.js` ו-`git init` · שינוי מילים שחן כתבה ·
החלטת מוצר, כלומר **מה** מסך עושה ולא איך הוא נראה · שינוי בהחלטה שהיא כבר קיבלה.

"כן", "אוקיי", "תמשיך", שאלה ושתיקה אינם אישור. רק "מאושר".
**בכל השאר לא שואלים.** תיקון טעות מאומתת, הפעלת סוכנים, אימות וגיבויים הם שלי.
פריט בודד שהוא החלטת מוצר, עוצרים עליו לבד וממשיכים עם השאר.

**כשאני נכנס לסשן ולא ברור מה באמצע:** שואל את חן על מה עובדים, לפני שאני פועל
לפי מסמך. מסמך מתאר עבר, חן מתארת הווה.
*נשבר 30.7: פעלתי לפי חסם ב-COMMITMENTS ופתחתי מחדש דיון עיצוב שנסגר מזמן.*

**כשאני מפעיל סוכן:** אם המשימה ויזואלית, הוא מקבל דפדפן. סוכן שקורא קוד בלבד
מוצא באגי לוגיקה, לא טעויות עיצוב. ויזואלי בלי דפדפן, לא מפעילים.
*נשבר 30.7: שלושה סוכנים סקרו 43 מסכים בלי לראות מסך אחד.*

**כשאני בודק ערך CSS:** קורא `getComputedStyle` בדפדפן. קריאת טקסט של CSS לא
תופסת cascade, לא תופסת כלל שני מאוחר יותר, ולא פותרת `color-mix` ו-`oklch`.
*נשבר שלוש פעמים ב-30.7.*

**כשאני עומד לדווח מספר לחן:** המספר מגיע מפקודה שהרצתי והפלט שלה מוצג.
"43 מתוך 43" בלי פלט הוא לא ממצא.

**כשסוכן מחזיר דוח:** אני מאמת כל ממצא לפני שהוא מגיע לחן. מה שלא אומת, לא עובר.
מה שנפסל, גם הפסילה מאומתת.

**כשאני נוגע ב-`app.js`:** קודם git. אין ריפו, אז קודם מבקשים אישור ל-`git init`.
בלי זה לא נוגעים.

**כשאני משנה קובץ קיים:** גיבוי לפני השינוי הראשון, לא אחריו.

**כשחן אומרת מילה שנשמעת כמו אישור:** אישור הוא ציטוט. "אוקיי", שאלה, או שתיקה
אינם אישור. נרשם ב-`plans/COMMITMENTS.md` בשדה `אישור: "..."`.

**כשאני מציע לחן לבחור בין אפשרויות:** בודק קודם שההחלטה לא כבר התקבלה. אם יש
סדר מוסכם, מזכיר אותו במקום להציע בחירה מדומה.
*נשבר 31.7: הצעתי בחירה בין תיקון מסכים לתיקון קוד, כשהסדר שחן קבעה כבר ענה על זה.*

**כשאני מוצא באג:** אוסף לרשימה. לא זורק אותם אחד-אחד תוך כדי עבודה.

**כשאני עומד למחוק או לארכב קובץ:** לא. חומר שחן יצרה נשאר. מבקשים אישור נפרד
לכל מחיקה.

**כשאני כותב לחן:** נקודה אחת בהודעה, שאלה אחת, בלי קיר טקסט. כשמבקשים תוכן,
נותנים תוכן, לא סיכום.

**כשאני מצהיר על משהו שאעשה:** נרשם ב-`plans/COMMITMENTS.md` ונבדק לפני שאני
מדווח שסיימתי.

**כשאני משנה את דרך העבודה:** נכתב לקובץ. אם לא נכתב, לא נאמר.

**כשאני נוגע במסך שקשור לסתירה פתוחה ב-`MAP.md` חלק ד:** הסתירה נסגרת קודם.

**כשאני כותב מסך חדש:** אין אימוג'י כאייקון, ואין `<style>` פנימי.
בדיקה: `grep -P "[\x{1F300}-\x{1FAFF}]" *.html` · `grep -l "<style" *.html`

## איך עובדים כאן

- **כל משימה מהותית עוברת לסאב-אייג'נט.** אני מפרק, מנסח בריף, ומאמת.
- סוכנים רצים ברקע ובמקביל.
- **סוכן מתקן, לא רק מדווח**, אלא אם הפעולה ברשימת האישורים למטה.
- כל בריף כולל: המסך `[חן]` של האזור, המפרט, ונעילה על `habit-flow.css`
  כשרצים כמה סוכנים במקביל. **נעילה, לא איסור. מי שצריך מחלקה חדשה מבקש ממני.**
- סוכן שנפל באמצע, העבודה שלו נבדקת ידנית לפני שממשיכים.

## דורש אישור של חן
- `git init` או כל שינוי ב-`app.js`
- מחיקה או ארכוב של קובץ
- שינוי בשורת הטאבים
- הוספת מסך שלא קיים ב-`MAP.md`

## לא נוגעים
- `node_modules/`, `screens/` — נוצרים אוטומטית
- `.backup-*/` — נקודת שחזור

## יחס לקובץ הגלובלי
`~/.claude/CLAUDE.md` מכיל את "כללי עבודה עם חן". **בסתירה, הקובץ הזה גובר**,
כי הוא ספציפי לפרויקט.
