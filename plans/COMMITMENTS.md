# התחייבויות

## תוקן ב-30.7: לא היתה שאלה של שפה ויזואלית

הקובץ הזה החזיק חסם בשם "איזו שפה ויזואלית היא הסטנדרט", שהתבסס על
הנחה שגויה: שהצילומים הכחולים ב-`_archive-old-mockups/` הם עיצובים של חן
שסותרים את המסכים החיים.

**הם לא. הם צילומי טסט מגרסת הקוד הישנה של האפליקציה.**
בלשונה של חן, 30.7:
> "המסכים הכחולים לא עוצבו כאן, הם היו טסטים כשיצרתי את האפליקציה בקוד.
> ברור שכל העיצובים החיים הם המקוריים."

**מסקנות:**
- **המסכים החיים (`.html`) הם מקור האמת לעיצוב.** אין חסם. שלב העיצוב סגור.
- הצילומים הישנים נשארים במקומם, לעיון בלבד. לא מקור אמת ולא מיושנים-למחיקה.
- **סתירות 3 ו-4 ב-`MAP.md` חלק ד בטלות.** שתיהן נשענו על הצילומים הישנים.
- הסתירות שנשארות אמיתיות, כי הן בין שני מסכים חיים:
  1 שני מסכי בוקר · 2 רוטציה מול `action-block` · 5 מסך זהות חסר · 6 שני מסכי תודות.

---


כל דבר שהצהרתי שאעשה. נבדק לפני שאני מדווח שסיימתי.
`[ ]` פתוח · `[x]` בוצע ואומת · `[-]` בוטל

## סדר העבודה המוסכם, 30.7

אישור: "1. לעבור על כל המסכים הקיימים ... 2. לעבור על הטקסטים ... 3. לעבור על ה-ux-ui ...
4. לעדכן את הקוד כי הקוד הקיים לא תואם"

1. [ ] **עקביות עיצוב** בכל המסכים החיים. אותו רקע, אותו כפתור, אותו הכל.
   - [x] **רקע.** אישור: "הרקע שנמצא במסך ה-36-habit-break.html זה הרקע שאני רוצה
     לכל המסכים באפליקציה". זה הרקע של `habit-flow.css`. הועתק לשבעת המסכים
     שהיו עם `<style>` פנימי. גיבוי: `.backup-bg-20260730/`.
     **האימות הראשון שלי היה שגוי.** בדקתי את הגדרת `.phone` הראשונה בכל קובץ,
     ובחמישה מסכים (01, 02, 04, 06, 07) ישבה הגדרה **שנייה** מאוחרת יותר שגברה עליה.
     דיווחתי לחן "43 מתוך 43" וזה לא היה נכון. סוכן ה-UX תפס את זה.
     תוקן. **האימות התקף: `getComputedStyle(.phone).backgroundImage` בדפדפן**,
     שמחזיר ערך זהה לייחוס `36-habit-break` בכל ששת המסכים שנבדקו.
     **לקח: בדיקה על טקסט של CSS לא תופסת cascade. בודקים ערך מחושב בדפדפן.**
   - [x] **גופן: Assistant.** אישור: "הגופן צריך להיות assistant".
     הוחלף Inter ב-Assistant ב-`habit-flow.css` ובשבעת המסכים. אומת בדפדפן:
     `document.fonts` מחזיר Assistant ב-400/500/600/700/800, עברית ולטינית.
   - [x] **שורת טאבים: זכוכית סגולה כהה.** אישור: "3 זכוכית סגולה כהה".
     הוחלה ב-`habit-flow.css` ובשלושת המסכים עם שורת טאבים משלהם: 06, 07,
     gratitude-writing. אומת: בליטת עיגול 29px מתוך 64, טשטוש 30px, גובה 76, בכל ארבעה.
     תוך כדי נשבר stroke של האייקונים ב-06 וב-07 ותוקן מיד.
   - [x] **מרווח עליון אחיד.** אישור: "תבצע את 1".
     `.content` ← `78px 32px 56px` ב-01/02/03/04. `.scroll` ← `14px 20px 16px` ב-06
     וב-gratitude-writing. `07-now-action` נשאר על `.main: 56px 28px 24px`, מבנה אחר.
   - [x] **ביקורת UX מלאה ומאומתת** → `plans/ux-audit-30-07.md`
     אישור: "תפעיל שוב סוכן ux-ui שיעבור על כל המסכים" · "תאמת הכל"
     שלושה סוכנים, 63 טענות, כולן אומתו שוב על ידי. שש נפסלו בטעות שלי וחזרו.
   - [x] **סבב תיקונים רץ לפי הדוח.** 28 מתוך 63 הממצאים כבר לא מתקיימים.
     נסרקו כל 43 המסכים בדפדפן אחרי השינוי: אפס שגיאות, אפס מסכים שבורים.
     **לא בוצע ודורש אישור נפרד:** מחיקת `today-morning` / `gratitude` /
     `gratitude-empty` / `action-block`, נוסח האזהרה על פנייה לאיש מקצוע,
     ושינוי טקסטים שחן כתבה.
   - [x] **דרגה 1: הרכיבים הבלתי נראים בגיליונות.** אישור: "תתחיל לתקן את המסכים" · "כן"
     בלוק אחד ב-`habit-flow.css`, וריאנט כהה בתוך `.sheet`. מתקן 8 מסכים.
     טוקן חדש `--accent-ink: #8a4bd6` לטקסט סגול על רקע בהיר.
     אומת בקנבס בדפדפן: הנמוך ביותר 4.84 מול תקן 4.5. לפני כן היו שניים על 1.00.
     גיבוי: `.backup-bg-20260730/habit-flow.css.pre-sheet`.
     **נסגרה גם שאלה פתוחה מהדוח:** הכותרת ב-`calm-tools` לא נחתכת.
2. [ ] **קופי** של כל האפליקציה
3. [ ] **UX/UI**
4. [ ] **עדכון הקוד ב-`app.js`** לפלאוו ולהתנהגות החדשים. דורש `git init` קודם.

**שלב אחד בכל פעם. לא מתחילים את הבא בלי אישור.**

**~~חריג, 30.7:~~ בוטל ב-31.7.** האישור הרץ ("כן, אתה יכול לעשות פשוט. הכל בלי
לשאול אותי") **אינו בתוקף.**

**הפרוטוקול מ-31.7: המילה "מאושר" היא הסימן היחיד להתחיל.**
אישור: "נסכם שאם אני אומרת מאושר זה הקיו שלך לעבוד" · "מאושר"
"כן", "אוקיי", "תמשיך", שאלה ושתיקה אינם אישור. עד שנאמר "מאושר", מציגים ולא מבצעים.
הכלל המלא ב-`~/my-day/CLAUDE.md`, ראשון ברשימת החוקים.

## פתוח

- [ ] **לבנות מסך זהות חדש באונבורדינג**
  אישור: "1. מסך חדש"
- [ ] **הכרטיס המתחלף נשאר**
  אישור: "אז לא לוותר עאליו"
- [ ] **לסגור שש סתירות לפני שנוגעים במסכים** (ראה MAP.md חלק ד)
- [ ] **גורל `action-block`** אחרי שהתברר שהרוטציה של 20 דקות היא הנכונה
- [ ] **לקרוא את 32 הרינדורים של חן שלא תועדו** ולתעד אותם ב-MAP
  אישור: "פשט תעשה מה שצריך כדי שיהיו לנו את כל המסכים מוכנים בלי סתירות בלי כפילויות"
- [x] **לבנות מחדש את תקועה ורגיעה כגיליונות** לפי העיצוב שלה.
  אומת 30.7: `stuck-choice` מכיל את חמש האפשרויות שלה כולל ״הקול בראש״ ו״אני בהצפה״,
  `calm-where` מכיל ״מגויסת / כבויה / בסדר, רק צריכה רגע״ עם הקרדיט Anchored · Deb Dana.
  שניהם גיליונות מעל רגע הפעולה, עם מעבר בלחיצה על כרטיס ובלי ״הלאה״.
  **כלומר סתירה 3 ב-MAP.md חלק ד סגורה בפועל.**
- [ ] **לאחד את שני מסכי הבוקר** (06 של חן מול today-morning)
- [ ] **לאחד את שני מסכי התודות** (gratitude-writing של חן מול gratitude)
- [ ] **להוסיף למצפן: תחושות הליבה, דף החזון, והחזרת ה-85%**
- [ ] **`git init` ב-`~/my-day` לפני שינוי קוד ראשון**

## בוצע

- [x] **פריוויו של כל המסכים עם הסבר מתחת לכל אחד** → `preview/index.html`
  אישור: "תציג לי בפריוויו את כל המסכים של האפליקציה עם הסבר מתחת" · "כן"
  אומת בדפדפן: 47 בלוקים, 40 רינדורים ו-47 מסגרות, כולן נטענות ב-200. 12 סתירות מסומנות.
- [x] גיבוי `~/my-day` לפני נגיעה בקוד → `.backup-20260729-1117/`
- [x] מפת פרויקט מלאה עם תוכן כל 43 המסכים → `MAP.md`
- [x] כללי עבודה ל-`~/.claude/CLAUDE.md`

## בוטל

- [-] מחיקת הכרטיס המתחלף מהבית. חן החליטה שהוא נשאר.
