# רשימת תיקונים מאומתת · 31 ביולי 2026

סוכן ויזואלי סרק את 43 המסכים בדפדפן. **כל שורה כאן אומתה שוב על ידי במדידה
בדפדפן לפני שנכנסה.** מה שלא אומת, לא ברשימה.

תיקייה: `~/Library/Application Support/Open Design/namespaces/release-stable/data/projects/brand-product-a4813f/`

---

## א · רגרסיות שנוצרו ב-30.7 ובתיקונים שלי. הן קודמות לכל השאר

### א1 · מוני האונבורדינג לא תואמים לנקודות
שיניתי תוויות ל"מתוך 6" ולא נגעתי בנקודות.

| קובץ | תווית | נקודות | פעילה |
|---|---|---|---|
| `02-onboarding-identity.html` | 2 מתוך 6 | **5** | 2 |
| `03-onboarding-goals.html` | 3 מתוך 6 | **5** | 3 |
| `26-habits-editor.html` | 4 מתוך 6 | **5** | 1 |
| `onboarding-break-habit.html` | 5 מתוך 6 | **4** | 2 |
| `04-onboarding-evening.html` | 6 מתוך 6 | **5** | **5** |

**התיקון:** שרשרת האונבורדינג היא שישה שלבים.
`01` ← `02` ← `03` ← `26` ← `onboarding-break-habit` ← `04`.
בכל חמשת הקבצים: **שש נקודות**, והפעילה לפי מספר השלב בתווית.

### א2 · `26-habits-editor.html` · שלושה מחוונים סותרים באותו מסך
תווית "4 מתוך 6" (שלב באונבורדינג), נקודות (שלב באונבורדינג), ופס התקדמות
`p20` (שלב פנימי בעורך).
**התיקון:** התווית והנקודות מתארות את האונבורדינג. **הפס מתאר את חמשת השלבים
הפנימיים ונשאר.** להוסיף מעל הפס תווית פנימית קטנה, `שלב 1 מתוך 5`, שמתעדכנת
עם הפס. בלי זה הפס נקרא כאילו הוא סותר את הנקודות.

### א3 · `07-now-action.html` · שורת הכפתורים לא מיושרת
מדוד: `play h64` · `← להחליף משימה h63` · `סיימתי h45`.
"סיימתי" נמוך ב-18 פיקסלים, ו"להחליף משימה" נשבר לשתי שורות.
**התיקון:** שלושת הפריטים באותו גובה. `סיימתי` ו`להחליף משימה` באותו רכיב
ובאותו גובה, ו`להחליף משימה` בשורה אחת. לקצר ל`להחליף` אם צריך.

---

## ב · קריטי

### ב1 · `compass-home.html` · תוכן נגלל מתחת לשורת טאבים אטומה
מדוד: `.white-card 711…809` חופף לשורת הטאבים שמתחילה ב-754, ולעיגול הצ׳אט
שמתחיל ב-725. שלושה בלוקי `.quiet` יושבים ב-819…954, מתחת למסגרת.
המכל **כן** נגלל (`overflow-y: auto`), אז התוכן לא אבד, אבל הוא עובר מתחת לשורה.
**התיקון:** `padding-bottom: 118px` על המכל הנגלל, כמו `.with-tabs` ב-`habit-flow.css`.

### ב2 · `calm-ground.html` · המונה מוצג הפוך
בקוד `"0 / 5"`, על המסך `5 / 0`. `direction: rtl` על מחרוזת שהיא מספרים ולוכסן.
**התיקון:** לעטוף את המונה ב-`<bdi dir="ltr">` או `<span dir="ltr">`.

### ב3 · `stuck-summary.html` · הספירה מוצגת הפוכה
בקוד `"5-4-3-2-1, מתחילה"`, על המסך `1-2-3-4-5, מתחילה`. אותה סיבה.
**התיקון:** לעטוף את `5-4-3-2-1` בלבד ב-`<bdi dir="ltr">`, ולהשאיר את
`, מתחילה` מחוץ לעטיפה.

**בדיקה לשני אלה:** לחפש בכל 43 המסכים מחרוזות של ספרות עם `-` או `/` שאינן
עטופות ב-`bdi`, ולתקן את כולן באותה צורה.

### ב4 · `25-habits.html` · שש נקודות ברשת הקולות בלתי נראות
מדוד: 6 מתוך 28 בצבע `oklch(… / 0.12)`, כלומר לבן ב-12%, על כרטיס לבן.
זו רשת הקולות, המנגנון המרכזי של המוצר. יום שהוחמץ אמור להיראות כעיגול ריק
ולא כחור.
**התיקון:** לנקודה כבויה בתוך כרטיס לבן, מסגרת `1px solid var(--border)`
ורקע שקוף. לא למלא אותה ולא לצבוע אותה באדום.

### ב5 · `calm-ladder.html` · כל שבע המדרגות במצב `active`
מדוד: 7 מתוך 7 `pill active`, רקע לבן על גיליון לבן. הן נראות כטקסט רגיל
ואין שום סימן שאפשר ללחוץ.
**התיקון:** להוריד `active` מכולן, ולתת ל-`.sheet .pill` מצב לא-נבחר נראה,
באותה שיטה שכבר קיימת ל-`.sheet .choice-card` ב-`habit-flow.css`.

### ב6 · `morning-build.html` · שורת הנקודות נוחתת על הכרטיס החמישי
מדוד: `.scroll` גולש ב-118px, `overflow: auto`, כלומר נגלל. אבל `.dots`
יושב באותו רוחב בדיוק מעל אזור הגלילה ומתנגש בתוכן.
**התיקון:** `padding-bottom` על אזור הגלילה בגובה שורת הנקודות ועוד 16px.

---

## ג · גבוה

### ג1 · `06-now-morning-full-v2.html` ו-`07-now-action.html` · סט אייקונים אחר
שם: `compass, heart, message, check-circle, clock(r=10)`.
ב-11 המסכים האחרים: `target(r=9), list, message, bulb, clock(r=9)`.
**"משימות" מקבל אייקון לב, ואותו לב משמש ב-06 עצמו לכרטיס "כתיבת תודה".**
**התיקון:** להעתיק את חמשת ה-SVG מ-`today-day.html` לשני הקבצים האלה.

### ג2 · שלושת מסכי התודות בלי טאב פעיל
`gratitude.html` · `gratitude-empty.html` · `gratitude-writing.html`.
בכל שאר מסכי הטאבים יש בדיוק אחד `tab active`.
**התיקון:** לסמן `הרגלים` כפעיל בשלושתם, כי תודות נכנסות דרך ההרגלים.

### ג3 · `onboarding-break-habit.html` · שדה השעה מיושר לשמאל
מדוד: השדה של `23:40` הוא `direction: ltr`, וארבעת האחים שלו `rtl`.
**התיקון:** `dir="ltr"` על **הערך** בלבד, בתוך `<bdi>`, והשדה עצמו נשאר `rtl`
כמו אחיו.

---

## ד · בינוני

### ד1 · `action-block.html` · חמש מתוך שש נקודות במצב `active`
מדוד: 5 גלולות של 30px ואחת של 9px. בכל מסך אחר בדיוק אחת פעילה.
**התיקון:** אחת פעילה. אם הכוונה הייתה זמן שחלף, להשתמש ב-`.progress-line`
ו-`.progress-fill.p83` שכבר קיימים ב-`habit-flow.css`, ולא ב-`.dots`.

### ד2 · `action-block.html` · הפעולה היחידה במסך מעוצבת כמשנית
`לעצור` הוא `cta secondary`, סגול על סגול.
**התיקון:** `cta` רגיל.

### ד3 · `stuck-question.html` · "הלאה" סגול ומשני
באותו תפקיד בדיוק, `03`, `24`, `26`, `36`, `morning-build`,
`onboarding-break-habit` ו-`compass-weekly` משתמשים ב-`cta` ירוק.
**התיקון:** `cta` ירוק, כמו כולם.

### ד4 · זוהר ירוק מתחת לכפתור סגול
`calm-glimmer.html` · `stuck-question.html` · `stuck-summary.html`.
מדוד: `box-shadow` בגוון ירוק על כפתור `rgb(155,93,229)`. **נראה בבירור בצילום.**
**התיקון:** צל הכפתור נגזר מצבע הכפתור.

### ד5 · `stuck-choice.html` ו-`stuck-fear.html` · כרטיסים גבוהים מדי
מדוד: כרטיס 84px לתוכן של 21px. חמישה כאלה יוצרים מסך כמעט ריק.
**התיקון:** `min-height` נמוך יותר לכרטיס `plain` בתוך גיליון. יעד: כ-64px.

### ד6 · שטח לבן ריק גדול בתוך כרטיס
`03-onboarding-goals.html`: `.goal-list` בגובה 211px עם שורה אחת של 64px.
`gratitude-writing.html`: `.gratitude-list` בגובה 250px עם שורה אחת של 54px.
**התיקון:** גובה המכל נגזר מהתוכן, עם מינימום נמוך.

### ד7 · הכפתור הראשי כמעט נוגע בעיגול הצ׳אט
`24-habits-empty` · `gratitude-empty` · `gratitude` · `tasks-empty`.
מדוד: `.cta` מסתיים ב-716, העיגול מתחיל ב-725. תשעה פיקסלים.
**התיקון:** מרווח 24px לפחות.

### ד8 · צד כפתור החזרה לא עקבי
ימין: `02`, `03`, `04`. שמאל: 12 מסכים אחרים.
**התיקון:** הצד שבו 12 המסכים משתמשים, בכל 15.

### ד9 · שוליים 20px במקום 32px
`06-now-morning-full-v2.html` ו-`gratitude-writing.html`.
מדוד: תוכן ב-`20…370`. ב-41 המסכים האחרים `32…358`.
**התיקון:** 32px.

### ד10 · `today-day.html` · גלולות התקועה והרגיעה ברוחב כפול
מדוד: 159px כל אחת, גובה 52. ב-`today-morning`, `today-evening`
ו-`today-closed`: 66px ו-61px, גובה 40.
**התיקון:** אותו גודל כמו בשלושת האחים.

### ד11 · `06-now-morning-full-v2.html` · המשימה השלישית כמעט בלתי קריאה
מדוד: לבן ב-alpha 0.92 כפול `opacity: 0.3`, כלומר כ-0.28 אפקטיבי, 14px על סגול.
**התיקון:** אטימות אפקטיבית של 0.55 לפחות. היא אמורה להיראות כלא-חובה, לא כמחוקה.

### ד12 · `calm-breath.html` ו-`calm-ground.html` · שורת הקרדיט בקושי נראית
מדוד: לבן ב-alpha 0.44, 11.5px, על סגול בינוני.
**התיקון:** להעלות ל-0.7 לפחות.

---

## ה · נמוך

- `artist-date.html`: הכרטיס האחרון ב-6px מהכפתור. שאר המרווחים במסך 14px. ליישר ל-14.
- `artist-date.html`: גלולות הימים ברוחבים 38, 37, 36, 37, 38, 34, 39. לרוחב אחיד.
- `tasks.html`: כותרת "מתישהו" בלי תוכן ובלי מצב ריק מתחתיה.
- `35-habit-resistance.html` ו-`compass-weekly.html`: כרטיס הערה לא לחיץ נראה
  זהה לכרטיס אופציה שנבחר. שניהם לבן מלא באותו טור.
- `stuck-summary.html`: ארבעה פריטים ברשימה אחת בשלושה עיצובים שונים.
- `today-morning.html`: שני כפתורים ירוקים ראשיים זהים באותו כרטיס.
- `04-onboarding-evening.html`: גלולת `22:00` יתומה בשורה שנייה משלה.
- `06-now-morning-full-v2.html` ו-`gratitude-writing.html`: שורת סטטוס
  (9:41, אנטנה, סוללה) קיימת רק בשני מסכים מתוך 43. או בכולם או באף אחד.

---

## מסכים שנמצאו תקינים ואין לגעת בהם

`01-onboarding-welcome` · `36-habit-break` · `action-celebrate` · `action-countdown`
· `action-move` · `compass-goal-editor` · `compass-horizons` · `compass-stuck`
· `today-closed` · `today-evening`

---

## מה שהסוכן טען ונפסל באימות

- **"`compass-home` לא נגלל".** נפסל. `overflow-y: auto`. התוכן נגיש בגלילה.
  החפיפה עצמה אמיתית ונשארה ברשימה.
- **"`morning-build` נחתך".** נפסל. גם הוא נגלל. ההתנגשות עם שורת הנקודות
  אמיתית ונשארה.
